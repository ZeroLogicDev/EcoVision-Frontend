/**
 * Route path constants — single source of truth.
 */
export const ROUTES = {
  LANDING: '/',
  LOGIN: '/masuk',
  AUTH_CALLBACK: '/auth/callback',
  DASHBOARD: '/dashboard',
  SCANNER: '/scanner',
  HISTORY: '/riwayat',
  PROFILE: '/profil',
};
